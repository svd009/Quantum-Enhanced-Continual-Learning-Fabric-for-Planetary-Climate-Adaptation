project:
  name: fedclimate
  seed: 42
  device: cpu
training:
  epochs: 100
  batch_size: 32
  learning_rate: 0.001
  weight_decay: 0.0001
  scheduler: cosine
logging:
  use_wandb: false
  log_every: 10
  save_every: 25
