@"
defaults:
  - base

federated:
  enabled: true
  num_rounds: 50
  num_clients: 3
  clients_per_round: 3
  local_epochs: 5
  aggregation: fedavg

ewc:
  enabled: true
  lambda: 5000
  fisher_samples: 200
"@ | Set-Content configs\federated.yaml
echo "done"